## 当前状态 (2026-05-29)

### 已完成
1. 场景3 ALC 模式已重构：使用直线参考线 y=0 + `target_lat_offset=-3.75`，规划器自主生成 d: 0→-3.75 变道轨迹
2. RViz2 黄色警告修复：删除零尺寸障碍物 CUBE + Marker lifetime 延长至 1.0-2.0s + Frame 0 DELETEALL
3. 历史轨迹持久化：新增 `persist_history` 参数，开启后生成 lifetime=∞ 的历史快照，运行结束后轨迹簇和最优轨迹永久保留在 RViz2 中
4. RViz2 视角跟随车辆：通过 tf2_ros::TransformBroadcaster 发布 map→base_link 变换，RViz Orbit 视图 Target Frame 设为 base_link
5. 黄色长方体拖影修复：ego marker ID 从 frame_count_ 改为固定值 0，1.0s 生命周期内不再累积
6. 画面闪烁修复：候选轨迹 marker ID 从 frame_count_*MAX 改为 cand_id（0-49），最优轨迹 ID 从 frame_count_ 改为 0，Marker 数量稳定
7. CMakeLists.txt 添加 INSTALL_RPATH 指向 conda yaml-cpp，无需手动设置 LD_LIBRARY_PATH
8. **仿真独立化** (9.1)：
   - 新建 `lattice_simulator_node` 独立可执行文件，100Hz 增量运动 + TF 广播 map→base_footprint
   - 提取 `scenario_manager.h/cpp` 管理场景配置，规划器和仿真器共用
   - `lattice_test_node` 删除 TF broadcast + init_scenario 方法，改为使用 scenario_manager
9. **可视化增强** (9.3)：
   - URDF 车辆模型 (`urdf/lattice_car.urdf`)：黄色底盘 3.0×1.6×1.5m + 4 个圆柱车轮
   - 道路边界 Marker (`/lattice_test/road_boundaries`)：左右白色边界线 + 黄色中线
   - RViz 升级：RobotModel Display + TF Display + ThirdPersonFollower 相机 (35m)
   - Launch 文件添加 robot_state_publisher + joint_state_publisher
10. **文档更新 + 运行验证**：
    - ARCHITECTURE.md：更新数据流图（规划器+仿真器分述）、新增关键修改记录 23-24
    - README.md：更新目录结构、架构图、可视化 topics 表（road_boundaries）、障碍物修改指南（指向 scenario_manager.cpp）、新增关键修改记录 23-24
    - 系统运行验证通过：5 进程全部正常，规划收敛 (d0: 0.50→0.00)
11. **ALC 换道结束状态机**（2026-05-29）：
    - `lattice_test_node.cpp` 新增 `alc_completed_flag_` + `alc_complete_debounce_counter_` 成员变量
    - 换道完成判定条件：`|d0 - target_d| < 0.20m` AND `|heading_error| < 0.03 rad` 连续 5 帧（0.5s 防抖）
    - 完成后执行量产级状态跳转：(a) 整条 `reference_line_` y 坐标平移 `target_d`；(b) `alc_completed_flag_` 守卫两处：`target_lat_offset = -3.75` 赋值 + `driving_mode = ALC` 赋值；(c) `driving_mode` 切回 LCC；(d) 重新发布参考路径 Path + 参考线 MarkerArray marker（DELETE old + ADD new）
    - **Bug 修复**：初版只守卫了 `target_lat_offset` 赋值，遗漏了 `driving_mode = ALC` 赋值，导致每帧 mode 被重置为 ALC 并无限重触发（每 5 帧一次 0.00m 空转）。参考线 MarkerArray marker 仅在 frame 0 发布一次，shift 后未更新导致蓝色参考线残留在旧位置
    - CLAUDE.md / README.md / ARCHITECTURE.md 同步更新
12. **Fast-DDS 共享内存已禁用 / CycloneDDS 切换**（2026-05-29）：
    - 安装 `ros-humble-rmw-cyclonedds-cpp`，Launch 文件默认设置 `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`
    - 从根本上解决 Fast-DDS SHM（`/dev/shm/fastrtps_*`）残留导致的 RViz 闪烁、轨迹残影和障碍物重影
    - `config/fastdds_no_shm.xml` 保留作为 Fast-DDS 纯 UDPv4 备用方案
13. **障碍物 Marker 持久化 / NUDGE 闪烁修复**（2026-05-29）：
    - 障碍物 CUBE 标记从每帧重发改为仅 frame 0 发布一次（`frame_locked=true`, `lifetime=Duration::max()`）
    - 根因分析：(a) 10Hz ADD 重发半透明障碍物（α=0.5）导致视觉闪烁；(b) NUDGE 模式下候选轨迹碰撞状态每帧切换（cyan/orange）引发连带闪烁；(c) 车辆通过障碍物后切换 LCC 模式，窄采样范围（[-0.3,0.3]）无碰撞切换，闪烁自然消失
    - 修复后障碍物与参考线、道路边界同为一次性静态发布
14. **Scenario 5: 4.2km S 弯高速赛道**（2026-05-29）：
    - 新增 `init_scenario_5()` — 从分段线性曲率剖面数值积分生成 4200m 连续 S 弯参考线（3 个弯道：500m R=左、700m R=右、1000m R=左，缓和曲线过渡）
    - ds=0.5m 分辨率，~8401 个参考点，无障碍物，LCC 默认模式
    - `scenario_manager.cpp` 中注册 case 5
15. **交互式换道**（2026-05-29）：
    - 新增 `/lattice_test/lane_change_cmd` 订阅（`std_msgs/Int8`, 1=左+3.75m, -1=右-3.75m）
    - `driving_mode_` 和 `target_lat_offset_` 从局部变量升级为成员变量，timer 回调和 subscription 回调共享状态
    - 换道进行中收到新指令时拒绝并打印 WARN
    - 换道完成后 `alc_completed_flag_` 仍为 true，由 subscription 回调重置以支持反复换道
16. **ALC 换道完成改为法向量平移**（2026-05-29）：
    - 从 `pt.set_y(pt.get_y() + target_d)` 升级为 `nx=-sin(heading_), ny=cos(heading_)` → `pt.x_ += target_d*nx; pt.y_ += target_d*ny`
    - 弯道（Scenario 5）上换道完成后参考线沿道路法向平移，不再简单改 y 坐标
    - 向后兼容：直线（heading=0）时 nx=0, ny=1，效果与旧代码完全一致
    - 换道完成后同时重发道路边界 markers（`publish_road_boundaries()`）
17. **道路边界渲染改为法向偏移**（2026-05-29）：
    - `publish_road_boundaries()` 中 `make_line` 和 `make_dashed_line` lambda：从 `rp.y_ + y_offset` 改为 `rp.x_ + y_offset*nx, rp.y_ + y_offset*ny`
    - 弯道上白线黄线跟随曲率正确偏移，不再在弯道外侧漂移
    - 双车道显示条件从 `scenario_ == 3` 扩展为 `scenario_ == 3 || scenario_ == 5`

18. **僵尸进程 / 闪烁跳动修复**（2026-05-29）：
    - 根因：前次 scenario 5 测试的僵尸进程（2 个 lattice_test_node + 2 个 lattice_simulator_node）持续发布 `/lattice_trajectory`，simulator 的防陈旧消息守卫只在首次轨迹检查，后续陈旧消息直接覆盖当前轨迹，导致车辆在正确位置和 S 弯末端（~3000m）之间反复跳变
    - `lattice_simulator_node.cpp`：陈旧轨迹检查改为每次消息都验证（与当前位置比较），不再是仅首次
    - 新增 `scripts/cleanup_stale.sh`：使用 `pgrep` + `kill`（排除自身 PID）清理过期进程和 Fast-DDS 共享内存
    - `run_scenario.launch.py`：cleanup 立即执行，所有节点通过 `TimerAction(period=0.5)` 延迟启动，确保清理在节点启动前完成

### 待办
-

### 使用
```bash
# 标准运行（含 3D 车辆模型 + 独立仿真节点）
ros2 launch lattice_standalone_test run_scenario.launch.py scenario:=1

# 开启历史持久化
ros2 launch lattice_standalone_test run_scenario.launch.py scenario:=1 persist_history:=true

# Scenario 5: 4.2km S 弯高速赛道
ros2 launch lattice_standalone_test run_scenario.launch.py scenario:=5

# 交互式换道（Scenario 5 运行时另开终端）
ros2 topic pub --once /lattice_test/lane_change_cmd std_msgs/msg/Int8 "data: 1"   # 左换道 +3.75m
ros2 topic pub --once /lattice_test/lane_change_cmd std_msgs/msg/Int8 "data: -1"  # 右换道 -3.75m
```
